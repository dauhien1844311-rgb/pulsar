# Pulsar 论文复现协作规范

## 目标（按阶段推进）
1. **第一阶段：最小 encode/decode 闭环跑通**
   - 能完成一次消息编码（encode）并输出 stego 图像。
   - 能从该 stego 图像中完成解码（decode）并回读原消息。
2. **第二阶段：复现论文主表指标**
   - 在与论文定义一致的设置下，统计并复现主表中的关键结果。

## 约束（必须遵守）
- **先最小改动**：优先通过配置、脚本、命令行参数完成复现。
- **不重构**：不进行与复现目标无关的代码结构调整。
- **不替换算法**：不更换核心算法实现，不引入“等价替代”实现。
- **不偷偷改实验定义**：不得私自更改论文中的任务定义、指标口径、数据切分、评测协议；如需变更，必须先记录并显式说明。

## 验收标准
复现工作至少应满足以下可验证结果：
1. 能生成至少一张 **stego 图像**。
2. 能从该图像 **解码回原消息**（与输入消息一致或按论文定义的一致性标准）。
3. 能统计并报告以下指标：
   - `bytes/image`
   - `success rate`
   - `throughput`
   - `encode time`
   - `decode time`

## 工作习惯
- **每完成一步**都要同步更新：
  - `REPRO_PLAN.md`（计划与下一步）
  - `REPRO_LOG.md`（实际执行、结果与问题）
- 若遇到缺失项（依赖 / 权重 / 数据）：
  - 先记录到 `REPRO_LOG.md`，明确缺失内容与阻塞影响；
  - **不要凭空假设**存在，不要“先按猜测继续”。
- 所有新增命令必须写成**可直接复制执行**的形式（含必要参数与路径）。

## 必备输出物
复现过程必须维护并最终提交以下文件：
- `REPRO_PLAN.md`
- `REPRO_LOG.md`
- `run_minimal.sh`
- `run_repro.sh`
- `RESULTS.md`

## 执行优先级
当时间或资源有限时，优先级如下：
1. 跑通最小闭环（encode/decode）
2. 打通指标统计链路
3. 复现论文主表并对齐报告
